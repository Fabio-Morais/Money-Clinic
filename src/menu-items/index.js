import dashboard from './dashboard';
import expenseswrap from './expenseswrap';
import other from './other';
import wallet from './wallet';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, other, wallet, expenseswrap]
};

export default menuItems;
